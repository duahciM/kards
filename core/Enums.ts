export enum PlayerId { Player1, Player2 }
export enum ZoneType { FriendlySupport, Frontline, EnemySupport }
export enum UnitType { Infantry, Tank, Artillery }
export enum Keyword {
  Flash = '闪击',
  Guard = '守护',
  Ambush = '伏击',
  Smoke = '烟幕',
}
